export function help(): string {
  return `ArgVault — redacted CLI run cassettes for agent handoffs\n\nUsage:\n  argvault init\n  argvault record [options] -- <command> [args...]\n  argvault report <cassette.json> [--out report.md]\n  argvault doctor\n\nRecord options:\n  --out <path>            JSON cassette path (default: .argvault/runs/latest.json)\n  --cwd <path>            workspace cwd (default: current directory)\n  --env <NAME>            capture one allowlisted env var; repeatable\n  --fixture <path>        attach fixture metadata; repeatable\n  --stdin-text <text>     feed text to the command\n  --stdin-file <path>     feed a workspace file to the command\n  --max-sample-bytes <n>  stdout/stderr/stdin sample cap\n  --note <text>           add a human note; repeatable\n\nExamples:\n  argvault record --env NODE_ENV --fixture fixtures/input.txt -- node scripts/repro.js\n  argvault report .argvault/runs/latest.json --out .argvault/runs/latest.md\n`;
}
